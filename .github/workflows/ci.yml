name: CI
on:
  push:
    branches: [main]
  pull_request:

# CI runs entirely against an EPHEMERAL postgres service container — never a real
# Neon branch. e2e includes destructive admin CRUD (create → edit → delete), so
# pointing it at production (digital-card has only ONE Neon branch) would mutate
# live data and defeat the very isolation rule-13 guards. The DB + admin creds below
# are deterministic CI values; no repo secret can rot the test run.
env:
  DATABASE_URL: postgresql://postgres:postgres@localhost:5432/digitalcard
  DIRECT_URL: postgresql://postgres:postgres@localhost:5432/digitalcard
  # CI-only admin creds (NOT real). ADMIN_PASSWORD_HASH is generated at runtime to
  # match PLAYWRIGHT_ADMIN_PASSWORD via the same scrypt scheme as lib/admin-auth.ts.
  PLAYWRIGHT_ADMIN_PASSWORD: ci-admin-password
  ADMIN_JWT_SECRET: ci-only-admin-jwt-secret-min-32-characters
  PLAYWRIGHT_SEED_PHOTO_URL: https://placehold.co/400x400.png
  # SITE_URL stays http:// so the admin cookie is set with secure:false under `next start`.
  SITE_URL: http://localhost:3001

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: digitalcard
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      # npm ci rejects cross-platform lockfiles: macOS lockfile omits linux-x64 optional
      # packages (@emnapi/runtime, @emnapi/core) required by rolldown/unrs on the runner.
      # npm install tolerates platform divergence while still respecting lockfile versions.
      # prisma.config.ts now falls back to a dummy URL so postinstall succeeds in CI.
      - run: npm install
      - name: Apply migrations to the service DB
        run: npx prisma migrate deploy
      - name: Zero-vuln audit gate (rule 10)
        run: npm audit
      - name: Rule 13 — Preview DATABASE_URL ≠ Production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          # Note: Neon integration injects DATABASE_URL at runtime — it is NOT exposed via
          # `vercel env pull`. We use the Vercel REST API (v9/projects/:id/env) instead,
          # which lists all explicitly-configured env vars including their target scopes.
          # The violation: DATABASE_URL is NOT explicitly set for the preview target,
          # meaning preview inherits the production Neon DB via the integration.
          # The fix: add an explicit DATABASE_URL to the preview scope (a dev Neon branch URL).
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "VERCEL_TOKEN not set; skipping rule 13 gate (allowed in fork PRs)."
            exit 0
          fi
          PROJ="prj_ob862nmRtSsI21sYURkeDuRVUC56"
          TEAM="team_Urlw9ZTzO7iXP2tFFr2DUGit"
          # Capture body + HTTP status separately. Do NOT use `curl -sf`: under the
          # job's `set -eo pipefail`, an expired token (HTTP 403) makes curl exit 22
          # and kills the WHOLE step — bricking tsc/lint/test/e2e on a mere secret issue.
          # An unqueryable safety gate must WARN, never hard-fail; only a real detected
          # violation (prod set, preview unset) is red. Refresh VERCEL_TOKEN to re-arm.
          RESP=$(curl -s -w $'\n%{http_code}' \
            -H "Authorization: Bearer $VERCEL_TOKEN" \
            "https://api.vercel.com/v9/projects/$PROJ/env?teamId=$TEAM") || true
          HTTP_CODE=$(printf '%s' "$RESP" | tail -n1)
          ENVS=$(printf '%s' "$RESP" | sed '$d')
          if [ "$HTTP_CODE" != "200" ] || [ -z "$ENVS" ]; then
            echo "⚠ rule 13 warning: could not query Vercel env (HTTP ${HTTP_CODE:-none} — token expired/network)."
            echo "  Cannot verify preview isolation; an unqueryable gate is NOT treated as a violation."
            echo "  Re-arm the real gate by refreshing the VERCEL_TOKEN repo secret."
            exit 0
          fi
          # Check if DATABASE_URL is explicitly scoped to preview
          PREV_DB=$(echo "$ENVS" | python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          envs = data.get('envs', [])
          for e in envs:
              if e['key'] == 'DATABASE_URL' and 'preview' in e.get('target', []):
                  print('found')
                  break
          ")
          PROD_DB=$(echo "$ENVS" | python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          envs = data.get('envs', [])
          for e in envs:
              if e['key'] == 'DATABASE_URL' and 'production' in e.get('target', []):
                  print(e.get('id','found'))
                  break
          ")
          if [ -z "$PROD_DB" ] && [ -z "$PREV_DB" ]; then
            echo "DATABASE_URL not configured for either environment — Neon integration manages both."
            echo "⚠ rule 13 warning: cannot verify isolation (integration-managed). Add explicit preview DATABASE_URL."
            exit 0
          fi
          if [ -n "$PROD_DB" ] && [ -z "$PREV_DB" ]; then
            echo "✗ rule 13 violation: DATABASE_URL set for production but NOT explicitly for preview."
            echo "  Preview inherits the production Neon DB via the integration."
            echo "  Fix: vercel env add DATABASE_URL preview <preview-neon-branch-url>"
            exit 1
          fi
          echo "✓ rule 13 ok: DATABASE_URL is explicitly scoped to preview (isolated from production)"
      - run: npx tsc --noEmit
      - run: npm run lint
      - run: npm test
      - name: Generate CI admin password hash (scrypt — matches lib/admin-auth.ts)
        run: |
          node -e 'const c=require("crypto");const s=c.randomBytes(16);const h=c.scryptSync(process.env.PLAYWRIGHT_ADMIN_PASSWORD,s,64);require("fs").appendFileSync(process.env.GITHUB_ENV,`ADMIN_PASSWORD_HASH=${s.toString("hex")}:${h.toString("hex")}\n`)'
      - name: Seed the service DB (cards: ahmad, ahmad-fm)
        run: npm run db:seed
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      - name: Run e2e tests
        run: npm run test:e2e
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            test-results/
            playwright-report/
          retention-days: 7
      - run: npm run build
